<% items.each do |item, subitems| %>
  <% if item.is_a?(QingGroup) %>
    <% if !subitems.empty? %>
      <%= render 'qing_groups/web', qing_group: item %>
      <%= render 'form_items/web', items: subitems %>
    <% end %>
  <% elsif item.printable? %>
    <%= render 'questionings/web', qing: item %>
  <% end %>
<% end %>
